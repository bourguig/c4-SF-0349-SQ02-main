{% extends 'base.html.twig' %}

{% block title %}Satisfaction{% endblock %}

{% block body %}
    <h1>Satisfaction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ satisfaction.id }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ satisfaction.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_satisfaction_index') }}">back to list</a>

    <a href="{{ path('app_satisfaction_edit', {'id': satisfaction.id}) }}">edit</a>

    {{ include('satisfaction/_delete_form.html.twig') }}
{% endblock %}
